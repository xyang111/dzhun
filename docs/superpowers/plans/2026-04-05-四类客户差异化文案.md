# 四类客户差异化文案 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 ScoreEngine V2.0 的 A/B/C/D 等级，让四类客户在结果页看到差异化的英雄区文案、产品排序逻辑和行动区块。

**Architecture:** 在 `renderMatchResult()` 函数顶部提取 `v2Level`（来自 `window._v2Result.level`），然后通过 `v2Level` 控制：①英雄区4种变体渲染；②产品列表排序/过滤策略；③行动区块的显示内容（B级损失框架/C级时间线/D级恢复路线图）。所有改动均限定在 `renderMatchResult()` 及其新增辅助函数内，不影响 ScoreEngine、支付、OCR 等其他模块。

**Tech Stack:** 原生 HTML/CSS/JS，无新依赖

---

## 文件变更清单

| 文件 | 操作 | 变更内容 |
|------|------|---------|
| `index.html` | 修改 | 在 `#convTop` 内替换 headline 结构；新增 `#convRehab`（D级路线图区块） |
| `style.css` | 修改 | 新增英雄区4变体样式、D级路线图样式 |
| `app.js` | 修改 | `renderMatchResult()` 顶部提取 `v2Level`；替换英雄区渲染逻辑；新增 `_renderHero()` 辅助函数；修改产品排序；D级隐藏 convLoss/convLift，渲染 rehab roadmap |

---

## Task 1: 提取 v2Level 并决定各区块显隐

修改 `renderMatchResult()` 开头，加入等级提取和区块控制逻辑。

**Files:**
- Modify: `app.js` 约 line 2128（`function renderMatchResult(r)` 开头）

- [ ] **Step 1: 在 renderMatchResult() 中 `const income = ...` 之前加入 v2Level 提取**

在 `app.js` line 2133（`const income = ...` 这行）之前，插入：

```js
  // ── V2.0 等级驱动的差异化渲染 ──
  const v2Level = (window._v2Result && window._v2Result.level) || _deriveLevel(r.cs_score || 0);
```

`_deriveLevel` 是 Task 2 会新增的辅助函数。

- [ ] **Step 2: 在 `renderMatchResult()` 末尾（line 2466，`document.getElementById('matchResult').style.display='block';` 之前）加入区块显隐控制**

```js
  // ── 按等级隐藏不相关区块 ──
  // A级：无需损失对比和优化路径（已是最优）
  if (v2Level === 'A') {
    const _lossEl = document.getElementById('convLoss');
    if (_lossEl) _lossEl.style.display = 'none';
  }
  // D级：隐藏损失对比和提升空间，显示恢复路线图
  if (v2Level === 'D') {
    const _lossEl = document.getElementById('convLoss');
    const _liftEl = document.getElementById('convLift');
    if (_lossEl) _lossEl.style.display = 'none';
    if (_liftEl) _liftEl.style.display = 'none';
    _renderRehabRoadmap(r);
  }
```

- [ ] **Step 3: 在 app.js 中 `renderMatchResult` 函数定义之前，新增辅助函数 `_deriveLevel`**

```js
function _deriveLevel(score) {
  if (score >= 800) return 'A';
  if (score >= 650) return 'B';
  if (score >= 500) return 'C';
  return 'D';
}
```

- [ ] **Step 4: 手动测试验证 v2Level 能正确读取**

打开浏览器控制台，在 `renderMatchResult` 被调用后运行：
```js
console.log(window._v2Result?.level, window._lastMatchResult?.cs_score);
```
预期：level 为 'A'/'B'/'C'/'D'，cs_score 为 300-1000 的数字。

---

## Task 2: 新增差异化英雄区渲染函数

替换 `convTop` 区块内的 headline 逻辑，改为调用 `_renderHero(v2Level, data)` 输出4种变体。

**Files:**
- Modify: `app.js` — 替换 `// ① 顶部总结区` 区块（line 2197–2229）
- Modify: `index.html` — 更新 `#convTop` 内部结构
- Modify: `style.css` — 新增英雄区样式

- [ ] **Step 1: 更新 index.html 中 #convTop 的结构**

当前 `convTop` 内部（从 `<div class="conv-eyebrow">` 到 `</div><!-- end convTop -->`）替换为：

```html
<div class="conv-top" id="convTop" style="display:none">
  <!-- 英雄区差异化内容由 _renderHero() 注入 -->
  <div id="heroContent"></div>
</div>
```

注意：`convCompareWrap`（通过率对比）和 `convNoIncomeHint` 整体移入 `heroContent`，由 JS 按需渲染，不再是固定 HTML。

- [ ] **Step 2: 在 style.css 末尾新增英雄区样式**

```css
/* ── 差异化英雄区 ── */
.hero-wrap { padding: 16px 0 8px; }
.hero-eyebrow { font-size: 9px; letter-spacing: .14em; text-transform: uppercase; margin-bottom: 8px; font-family: 'JetBrains Mono', monospace; }
.hero-title { font-size: 20px; font-weight: 700; color: #fff; margin-bottom: 6px; line-height: 1.3; }
.hero-sub { font-size: 12px; margin-bottom: 14px; }
.hero-metrics { display: grid; gap: 8px; margin-bottom: 12px; }
.hero-metrics.cols-3 { grid-template-columns: 1fr 1fr 1fr; }
.hero-metrics.cols-2 { grid-template-columns: 1fr 1fr; }
.hero-metric { border-radius: 5px; padding: 10px; text-align: center; }
.hero-metric-label { font-size: 10px; color: #6b7280; margin-bottom: 3px; }
.hero-metric-val { font-size: 17px; font-weight: 700; }
.hero-metric-sub { font-size: 10px; margin-top: 2px; color: #9ca3af; }
.hero-note { border-radius: 5px; padding: 10px 13px; font-size: 12px; }
/* A级 绿色 */
.hero-a .hero-eyebrow { color: #4ade80; }
.hero-a .hero-sub { color: #86efac; }
.hero-a .hero-metric { background: #0a2a0a; }
.hero-a .hero-metric-val { color: #4ade80; }
.hero-a .hero-note { background: #0a2a0a; border: 1px solid rgba(74,222,128,.25); color: #86efac; }
/* B级 蓝色 */
.hero-b .hero-eyebrow { color: #60a5fa; }
.hero-b .hero-sub { color: #93c5fd; }
.hero-b .hero-metric { background: #0d1a2b; }
.hero-b .hero-metric-val { color: #fff; }
.hero-b .hero-metric-val.gain { color: #4ade80; }
.hero-b .hero-note { background: #0d1a2b; border: 1px solid rgba(248,113,113,.25); color: #fca5a5; }
/* C级 琥珀色 */
.hero-c .hero-eyebrow { color: #fbbf24; }
.hero-c .hero-sub { color: #fde68a; }
.hero-c .hero-metric { background: #2a1e00; }
.hero-c .hero-metric-val { color: #fbbf24; }
.hero-c .hero-metric-val.locked { color: rgba(74,222,128,.5); }
.hero-c .hero-note { background: #2a1e00; border: 1px solid rgba(251,191,36,.25); color: #fde68a; }
/* D级 红色 */
.hero-d .hero-eyebrow { color: #f87171; }
.hero-d .hero-sub { color: #fca5a5; }
.hero-d .hero-metric { background: #2a0d0d; }
.hero-d .hero-metric-val { color: #f87171; }
.hero-d .hero-metric-val.milestone { color: #fbbf24; }
.hero-d .hero-metric-val.recovery { color: rgba(74,222,128,.6); }
.hero-d .hero-note { background: #2a0d0d; border: 1px solid rgba(248,113,113,.25); color: #fca5a5; }
```

- [ ] **Step 3: 在 app.js 中新增 `_renderHero()` 函数（放在 `renderMatchResult` 之前）**

```js
function _renderHero(level, r, cp, op, gapW, curAmt, optAmt) {
  const el = document.getElementById('heroContent');
  if (!el) return;
  const score = (window._v2Result && window._v2Result.score) || r.cs_score || 0;
  const scoreDisp = score > 0 ? score : '--';

  const _metricBox = (label, val, cls, sub) =>
    `<div class="hero-metric"><div class="hero-metric-label">${label}</div>` +
    `<div class="hero-metric-val${cls ? ' ' + cls : ''}">${val}</div>` +
    (sub ? `<div class="hero-metric-sub">${sub}</div>` : '') +
    `</div>`;

  if (level === 'A') {
    el.innerHTML = `<div class="hero-wrap hero-a">
      <div class="hero-eyebrow">A级 · PREMIUM ACCESS</div>
      <div class="hero-title">您已进入银行优质准入区间</div>
      <div class="hero-sub">征信状态优质 · ${cp}款产品可直接申请 · 利率可谈至最低档</div>
      <div class="hero-metrics cols-3">
        ${_metricBox('可申请最低利率', '3.0%', '')}
        ${_metricBox('综合评分', scoreDisp, '')}
        ${_metricBox('符合产品数', cp + '款', '')}
      </div>
      <div class="hero-note">💎 <strong style="color:#fff">专属白名单通道：</strong>您的资质符合银行优先审批条件，最低利率需人工对接谈判</div>
    </div>`;
    return;
  }

  if (level === 'B') {
    const nOpt = Array.isArray(r.optimization) ? r.optimization.length : 3;
    const gainText = gapW > 0 ? `多拿 <span style="color:#4ade80">${gapW}万</span> 额度`
                               : `多申请 <span style="color:#4ade80">${op - cp}款</span> 产品`;
    el.innerHTML = `<div class="hero-wrap hero-b">
      <div class="hero-eyebrow">B级 · OPTIMIZATION GAP</div>
      <div class="hero-title">做${nOpt}个优化，可以${gainText}</div>
      <div class="hero-sub">当前资质符合${cp}款产品 · 优化后可达${op}款 · 最快3个月见效</div>
      <div class="hero-metrics cols-2">
        ${_metricBox('当前可贷额度', curAmt !== '填写收入后显示' ? curAmt + '万' : curAmt, '')}
        ${_metricBox('优化后可达', optAmt !== '填写收入后显示' ? optAmt + '万 <span style="font-size:11px">↑多' + gapW + '万</span>' : optAmt, 'gain')}
      </div>
      <div class="hero-note">⚡ 申请顺序搞错会多等3个月 · 客服给你精准执行计划</div>
    </div>`;
    return;
  }

  if (level === 'C') {
    el.innerHTML = `<div class="hero-wrap hero-c">
      <div class="hero-eyebrow">C级 · RECOVERY PATH</div>
      <div class="hero-title">当前有 <span style="color:#fbbf24">${cp}款</span> 产品可立即申请</div>
      <div class="hero-sub">3–6个月优化后，可进入主流股份制银行区间</div>
      <div class="hero-metrics cols-3">
        ${_metricBox('现在可申请', cp + '款', '', '城商行 + 消金')}
        ${_metricBox('3个月后解锁', '+9款 🔒', 'locked', '股份制银行')}
        ${_metricBox('6个月后解锁', '+6款 🔒', 'locked', '国有大行')}
      </div>
      <div class="hero-note">📋 过渡方案可用，但申请顺序很关键 · 顺序错了影响后续恢复</div>
    </div>`;
    return;
  }

  // D级
  el.innerHTML = `<div class="hero-wrap hero-d">
    <div class="hero-eyebrow">D级 · REHABILITATION PLAN</div>
    <div class="hero-title">银行通道暂时关闭</div>
    <div class="hero-sub">专属征信修复路线图已生成 · 预计 <strong style="color:#fff">9个月</strong> 后重新达到银行准入</div>
    <div class="hero-metrics cols-3">
      ${_metricBox('当前保底方案', '2款消金', '')}
      ${_metricBox('第一里程碑', '第3个月', 'milestone')}
      ${_metricBox('恢复银行准入', '第9个月', 'recovery')}
    </div>
    <div class="hero-note">🤝 恢复期全程陪伴 · 每月进度同步 · 不是一次性报告</div>
  </div>`;
}
```

- [ ] **Step 4: 替换 app.js 中 `// ① 顶部总结区` 区块（约 line 2197–2229）**

将整个 `// ① 顶部总结区` 区块（从 `const topEl = document.getElementById('convTop');` 到 对应的 `}` ）替换为：

```js
  // ① 顶部英雄区（差异化）
  const topEl = document.getElementById('convTop');
  if (topEl) {
    topEl.style.display = 'block';
    const cp = products.length;
    const op = Math.max(cp, r.optimized_products || (cp + 2));
    _renderHero(v2Level, r, cp, op, gapW, curAmt, optAmt);
  }
```

注意：`cp` 和 `op` 在后面的 `convLift` 区块也有局部定义（line 2277–2278），不冲突（各自作用域）。

- [ ] **Step 5: 手动测试4个等级的英雄区**

在控制台模拟4个等级（临时覆盖 `window._v2Result`）：
```js
// 测试A级
window._v2Result = { level: 'A', score: 850 };
renderMatchResult(window._lastMatchResult);
// 查看 #heroContent 是否显示绿色 PREMIUM ACCESS 变体
```
重复对 B（score:700）/ C（score:560）/ D（score:380）执行相同操作，逐一核验。

- [ ] **Step 6: Commit**

```bash
git add index.html style.css app.js
git commit -m "feat: 四类客户差异化英雄区 - A/B/C/D 四种变体"
```

---

## Task 3: A级产品列表按利率升序排列

A级客户的核心需求是"比价"，产品列表改为按利率从低到高排序。

**Files:**
- Modify: `app.js` — 产品分组渲染区块（约 line 2427–2436）

- [ ] **Step 1: 找到产品渲染 `else` 分支（约 line 2427）**

当前代码：
```js
  } else {
    const _bigBank  = products.filter(p=>p.type==='bank'&&(p.tags||[]).includes('国有大行'));
    const _othBank  = products.filter(p=>p.type==='bank'&&!(p.tags||[]).includes('国有大行'));
    const _finProds = products.filter(p=>p.type!=='bank');
    let _gridHtml='';
    if(_bigBank.length)  _gridHtml += _tierHd('国有大行','优先申请，利率最低') + _bigBank.map(_mkCard).join('');
    if(_othBank.length)  _gridHtml += _tierHd('股份制 / 城商行','') + _othBank.map(_mkCard).join('');
    if(_finProds.length) _gridHtml += _tierHd('消费金融','备选，最后申请') + _finProds.map(_mkCard).join('');
    document.getElementById('productsGrid').innerHTML=_gridHtml;
  }
```

- [ ] **Step 2: 替换为支持 v2Level 的版本**

```js
  } else {
    const _parseRate = p => parseFloat((p.rate || '').replace('%','').replace('起','')) || 99;
    let _gridHtml = '';
    if (v2Level === 'A') {
      // A级：全部产品按利率升序，单一列表不分层（便于比价）
      const _sorted = [...products].sort((a, b) => _parseRate(a) - _parseRate(b));
      _gridHtml = _tierHd('全部产品', '已按利率从低到高排序') + _sorted.map(_mkCard).join('');
    } else if (v2Level === 'D') {
      // D级：只展示消费金融保底产品，不显示大量被拒产品
      const _fallback = products.filter(p => p.type !== 'bank');
      if (_fallback.length > 0) {
        _gridHtml = _tierHd('当前可申请', '银行产品暂不可申请，以下为保底方案') + _fallback.map(_mkCard).join('');
      } else {
        _gridHtml = `<div style="padding:16px;text-align:center;color:var(--muted);font-size:13px">当前暂无可直接申请的产品<br>请参考下方恢复路线图</div>`;
      }
    } else {
      // B/C级：保持原有三层分组
      const _bigBank  = products.filter(p=>p.type==='bank'&&(p.tags||[]).includes('国有大行'));
      const _othBank  = products.filter(p=>p.type==='bank'&&!(p.tags||[]).includes('国有大行'));
      const _finProds = products.filter(p=>p.type!=='bank');
      if(_bigBank.length)  _gridHtml += _tierHd('国有大行','优先申请，利率最低') + _bigBank.map(_mkCard).join('');
      if(_othBank.length)  _gridHtml += _tierHd('股份制 / 城商行','') + _othBank.map(_mkCard).join('');
      if(_finProds.length) _gridHtml += _tierHd('消费金融','备选，最后申请') + _finProds.map(_mkCard).join('');
    }
    document.getElementById('productsGrid').innerHTML=_gridHtml;
  }
```

- [ ] **Step 3: 验证 A级产品排序**

控制台设置 `window._v2Result = { level: 'A', score: 850 }` 后触发 `renderMatchResult(window._lastMatchResult)`，查看 `#productsGrid` 中产品是否按利率升序排列，且无分层标题（只有"全部产品"一个 header）。

- [ ] **Step 4: 验证 D级只显示消费金融**

控制台设置 `window._v2Result = { level: 'D', score: 380 }` 后触发 `renderMatchResult(window._lastMatchResult)`，验证 `#productsGrid` 中只有消费金融/保底产品，无银行产品。

- [ ] **Step 5: Commit**

```bash
git add app.js
git commit -m "feat: A级产品按利率排序，D级只显示保底产品"
```

---

## Task 4: D级恢复路线图区块

D级客户不展示提升空间（convLift），改为显示9个月恢复路线图。

**Files:**
- Modify: `index.html` — 在 `#convLift` 之后新增 `#convRehab`
- Modify: `style.css` — 新增路线图样式
- Modify: `app.js` — 新增 `_renderRehabRoadmap()` 函数

- [ ] **Step 1: 在 index.html 的 `#convLift` 结束标签后面插入 `#convRehab`**

在 `<!-- 客户标签区 -->` 注释之前，插入：

```html
<!-- D级恢复路线图 -->
<div class="rehab-sec" id="convRehab" style="display:none">
  <div class="conv-sec-hd"><span class="conv-sec-title">征信恢复路线图</span></div>
  <div id="rehabContent"></div>
</div>
```

- [ ] **Step 2: 在 style.css 末尾追加路线图样式**

```css
/* ── D级恢复路线图 ── */
.rehab-sec { margin-bottom: 16px; }
.rehab-timeline { display: flex; flex-direction: column; gap: 0; }
.rehab-step { display: flex; gap: 12px; }
.rehab-left { display: flex; flex-direction: column; align-items: center; flex-shrink: 0; width: 32px; }
.rehab-dot { width: 12px; height: 12px; border-radius: 50%; border: 2px solid; flex-shrink: 0; margin-top: 4px; }
.rehab-dot.now { border-color: #f87171; background: #f87171; }
.rehab-dot.m3  { border-color: #fbbf24; background: transparent; }
.rehab-dot.m6  { border-color: rgba(74,222,128,.5); background: transparent; }
.rehab-dot.m9  { border-color: #4ade80; background: transparent; }
.rehab-vline { flex: 1; width: 1px; background: var(--border); margin: 4px 0; min-height: 28px; }
.rehab-body { padding: 0 0 20px; flex: 1; }
.rehab-month { font-size: 10px; color: var(--muted); letter-spacing: .08em; margin-bottom: 3px; }
.rehab-task { font-size: 13px; font-weight: 600; color: var(--white); margin-bottom: 3px; }
.rehab-unlock { font-size: 11px; color: #9ca3af; }
.rehab-unlock.active { color: #4ade80; }
```

- [ ] **Step 3: 在 app.js 中新增 `_renderRehabRoadmap()` 函数（放在 `_renderHero` 定义旁边）**

```js
function _renderRehabRoadmap(r) {
  const secEl = document.getElementById('convRehab');
  const contentEl = document.getElementById('rehabContent');
  if (!secEl || !contentEl) return;
  secEl.style.display = 'block';

  const steps = [
    {
      dotCls: 'now',
      month: '当前 · 立即执行',
      task: '停止所有贷款申请，避免查询次数增加',
      unlock: '保底可申请：消费金融保底方案（2款）',
      unlockCls: 'active',
      hasLine: true,
    },
    {
      dotCls: 'm3',
      month: '第3个月',
      task: '查询冷却完成 + 完成1笔按时还款记录',
      unlock: '解锁：城商行产品（+4款）',
      unlockCls: '',
      hasLine: true,
    },
    {
      dotCls: 'm6',
      month: '第6个月',
      task: '持续良好还款记录，网贷机构降至安全线',
      unlock: '解锁：股份制银行产品（+9款）',
      unlockCls: '',
      hasLine: true,
    },
    {
      dotCls: 'm9',
      month: '第9个月 · 目标节点',
      task: '征信状态恢复至C级，可申请主流银行',
      unlock: '解锁：国有大行产品，利率可达5%-7%',
      unlockCls: 'active',
      hasLine: false,
    },
  ];

  contentEl.innerHTML = `<div class="rehab-timeline">` +
    steps.map(s => `
      <div class="rehab-step">
        <div class="rehab-left">
          <div class="rehab-dot ${s.dotCls}"></div>
          ${s.hasLine ? '<div class="rehab-vline"></div>' : ''}
        </div>
        <div class="rehab-body">
          <div class="rehab-month">${s.month}</div>
          <div class="rehab-task">${s.task}</div>
          <div class="rehab-unlock ${s.unlockCls}">${s.unlock}</div>
        </div>
      </div>`
    ).join('') +
    `</div>`;
}
```

- [ ] **Step 4: 验证 D级路线图渲染**

控制台设置 `window._v2Result = { level: 'D', score: 380 }` 后触发 `renderMatchResult(window._lastMatchResult)`，验证：
- `#convLoss` 和 `#convLift` 均隐藏（`display:none`）
- `#convRehab` 显示，内含4步时间线
- 4个 `rehab-dot` 颜色分别为红/琥珀/淡绿/绿

- [ ] **Step 5: Commit**

```bash
git add index.html style.css app.js
git commit -m "feat: D级恢复路线图区块 - 9个月四步时间线"
```

---

## Task 5: B级提升空间区块改为收益框架

当前 `convLift` 的标题是"你的提升空间"，对B级改为量化收益对比。

**Files:**
- Modify: `app.js` — `// ④ 提升空间` 区块（约 line 2267–2286）

- [ ] **Step 1: 找到 convLift 的标题更新逻辑，在其中加入 B级专属标题**

在 `// ④ 提升空间` 区块内、`liftEl.style.display='block';` 之后插入：

```js
    // B级：改为收益框架标题
    if (v2Level === 'B') {
      const _liftTitle = liftEl.querySelector('.conv-sec-title');
      if (_liftTitle) _liftTitle.textContent = '做这几步，可以多拿这些';
    }
```

- [ ] **Step 2: 验证 B级标题变化**

控制台 `window._v2Result = { level: 'B', score: 700 }` 后重新渲染，查看 `convLift` 的 `.conv-sec-title` 文字是否变为"做这几步，可以多拿这些"；A/C/D级仍显示"你的提升空间"或被隐藏。

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat: B级提升空间标题改为收益框架文案"
```

---

## Task 6: CTA 文案差异化

`#convCtaSub`（转化区副标题）按等级显示不同的文案。

**Files:**
- Modify: `app.js` — `// ⑧ 转化区` 区块（约 line 2356–2365）

- [ ] **Step 1: 替换 ctaSub 赋值逻辑**

当前代码（约 line 2360–2364）：
```js
  const ctaSub=document.getElementById('convCtaSub');
  if(ctaSub){
    if(_tier==='bank')
      ctaSub.innerHTML='顾问协助深度解析官方持牌金融机构准入政策<br>精准匹配申贷方案，通过算法优化降低综合财务成本';
    else
      ctaSub.innerHTML='这些因素可能影响你的最终额度和通过率<br>联系顾问获取针对你的定制方案';
  }
```

替换为：
```js
  const ctaSub = document.getElementById('convCtaSub');
  if (ctaSub) {
    const _ctaCopy = {
      A: '专属白名单通道需人工对接，顾问协助谈判最低利率',
      B: '申请顺序搞错会多等3个月，顾问给你精准执行计划',
      C: '有过渡方案，但申请顺序很关键，顺序错了影响后续恢复',
      D: '恢复期全程陪伴，每月进度同步，不是一次性报告',
    };
    ctaSub.textContent = _ctaCopy[v2Level] || _ctaCopy['B'];
  }
```

- [ ] **Step 2: 验证4个等级的CTA文案**

逐一设置 v2Level 为 A/B/C/D 后重新渲染，确认 `#convCtaSub` 文字对应正确。

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat: CTA副标题按A/B/C/D等级差异化"
```

---

## Task 7: 端到端验证与部署

- [ ] **Step 1: 完整流程测试**

在本地 `file://` 或 nginx 上打开 `index.html`，完成一次完整流程（上传征信图、填写信息、完成支付、触发匹配），确认：
- 付费后结果页正常显示，英雄区显示对应等级变体
- 控制台无报错

- [ ] **Step 2: 等级边界测试**

若无真实征信图，在控制台执行以下测试（在 `startMatching()` 完成后）：
```js
// 测试A级渲染
window._v2Result = { level: 'A', score: 850 };
renderMatchResult(window._lastMatchResult);
// 检查：heroContent 有 .hero-a，productsGrid 产品按利率升序，convLoss 隐藏

// 测试D级渲染
window._v2Result = { level: 'D', score: 380 };
renderMatchResult(window._lastMatchResult);
// 检查：heroContent 有 .hero-d，productsGrid 只有消金产品，convRehab 显示，convLoss/convLift 隐藏
```

- [ ] **Step 3: 部署**

```bash
./deploy.sh
```

- [ ] **Step 4: 线上冒烟测试**

访问 `https://dzhun.com.cn`，完成一次完整流程，确认结果页正常，浏览器控制台无 JS 报错。
